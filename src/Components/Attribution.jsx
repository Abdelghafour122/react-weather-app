import { Typography } from "@mui/material";
import { Box } from "@mui/system";
import React from "react";

const Attribution = () => {
  return (
    <Box>
      <Typography variant="p">Coded with </Typography>
      <Typography></Typography>
    </Box>
  );
};

export default Attribution;
